export interface VideoItem {
  id: string;
  title: string;
  artist: string;
  songTitle: string;
  duration: string;
  coverUrl: string;
  videoUrl?: string;
  description?: string;
  genre?: string;
  year?: string;
}
